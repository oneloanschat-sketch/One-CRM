services:
  - type: web
    name: one-mortgages-crm
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: API_KEY
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html